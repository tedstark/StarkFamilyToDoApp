extends page_layout

block content
  h1 #{title}
  ul.list-group
      ol.list-group-item
        each task, i in tasks
          li.list-group-item-heading
            if user
              a(href="/tasks/view/"+task._id)= task.tasktitle + ' for '+ task.assignedto
              if (task.assignedto ==user.fullname || user.role =='parent') && task.complete ==false
                a.btn.btn-success.btn-xs.float-sm-right.complete-task(href='#', data-id=task._id) Done!
              if user.fullname ==task.creator || user.role =='parent'
                a.btn.btn-info.btn-xs.float-sm-right.edit-task(href="/tasks/edit/"+task._id) Edit
                a.btn.btn-danger.btn-xs.float-sm-right.delete-task(href='#', data-id=task._id) Del
  br
  if user
    if user.fullname =='Ted Stark'      
      p Fixes/enhancements for this page:
        ol
          li 
            del Place info, delete, complete buttons on task rows based on User Roles
          li Present different views for different Roles
          li Set up task list in columns in some way
          li Set up confirmation message/feedback for Delete button.
          li Fix redirect/render for Delete button to refresh the Task List page.
          li Put formatted due date into task list.
